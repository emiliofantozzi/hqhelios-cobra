  # ===========================================
     # SUPABASE - BASE DE DATOS
     # ===========================================

     # Conexión con PgBouncer (usar para queries en runtime)
     DATABASE_URL=

     # Conexión directa (usar para migraciones Prisma)
     DIRECT_URL=

     # ===========================================
     # SUPABASE - API
     # ===========================================

     # URL pública del proyecto
     NEXT_PUBLIC_SUPABASE_URL=

     # Clave anónima (segura para cliente)
     NEXT_PUBLIC_SUPABASE_ANON_KEY=

     # Clave de servicio (⚠️ SOLO SERVER-SIDE, NUNCA EXPONER)
     SUPABASE_SERVICE_ROLE_KEY=

     # ===========================================
     # CLERK - AUTENTICACIÓN
     # ===========================================

     # Clave pública (segura para cliente)
     NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=

     # Clave secreta (⚠️ SOLO SERVER-SIDE)
     CLERK_SECRET_KEY=

     # Secreto para validar webhooks de Clerk (Story 1.2)
     CLERK_WEBHOOK_SECRET=

     # Rutas de autenticación
     NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
     NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
     NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
     NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

     # ===========================================
     # VERCEL - DEPLOYMENT
     # ===========================================

     # Token para deploy desde MCP/CLI
     VERCEL_TOKEN=

# ===========================================
# RESEND - EMAIL
# ===========================================

RESEND_API_KEY=
RESEND_FROM_EMAIL=
RESEND_FROM_NAME=
RESEND_WEBHOOK_SECRET=